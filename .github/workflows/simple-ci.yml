name: sample-ci
on: push
jobs:
    test-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4
              
            - name: list repository files
              run: ls -la
            - name: print README constent
              run: cat README.md

